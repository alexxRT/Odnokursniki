#include "client.h"
#include "gui_client.h"

window.event;

while ( 1 ) { // main loop

    if ( event == WiNDOW_EVENT ) {

        switch( event ) {

            case ( Pressed_button ): 
                    switch ( button ){
                        case ( isalpha ):   ADD_Letter( std::string message ); // and draw
                        case ( isenter ):   SEND_MESSAGE;

                    }


            default:

        }

    }

    recieve( msg, socket );
    if ( msg ) draw 


}

std::vector< std::vector<char> > users ( name max := 10)
std::vector< std::vector<char> > messages ( "TAG_/*SIMPLEMSG_FROM_TO_MSG*/_/*ONLINE-user_names*/_/*legacy_from_from_to*/")
                                            // tag - msg type 


int main (){

    client client (4784);

    cliend.see_online();
    client.sent_to(from, to , message);
    client.recv_msg();




    return 0;
}